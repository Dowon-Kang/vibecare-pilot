# VibeCare 제품·디자인 규약

상태: Flutter 모바일 `pilot-0.6.0` / Mock 시뮬레이션 전용
최종 갱신: 2026-09-10

## 1. 왜 만드는가

VibeCare는 고령 참여자의 최근 체성분 측정 4건과 당일 안전 문진을 근거로 연구용 진동 설정을 계산하고, 사용자가 **오늘 실행 가능한지**, **어떤 값이 적용되는지**, **실행할 수 없다면 왜 그런지**를 실수 없이 확인하도록 돕는 도구다.

이 앱의 성공 기준은 화면이 화려해 보이는 것이 아니다.

1. 사용자가 10초 안에 현재 상태(사용 가능·확인 필요·사용 중지)를 이해한다.
2. 안전 문진에 명시적으로 답하지 않으면 실행 명령을 만들 수 없다.
3. 원본 측정값, 평균값, 보정계수, 최종값을 역추적할 수 있다.
4. Mock과 실제 기기 연결 상태를 혼동하지 않는다.
5. 시력·운동·인지 능력이 저하된 사용자도 핵심 흐름을 완료할 수 있다.

## 2. 사용자와 사용 맥락

- 1차 사용자: 고령 연구 참여자 또는 현장에서 참여자를 돕는 담당자
- 사용 시점: 체성분 측정값이 준비된 뒤, 연구용 설정을 검토하고 Mock 흐름을 시연할 때
- 주요 불안: 어떤 값이 선택됐는지, 몸 상태가 좋지 않을 때 중단되는지, 버튼을 누르면 실제 장치가 움직이는지
- 핵심 과업: `대상자 확인 → 측정 4건 확인 → 안전 문진 → 추천 확인 → Mock 설정 준비/시연`
- 현재 기술적 사실: Flutter 앱은 Mock 측정값으로 추천·전송·실행·중지 흐름을 시뮬레이션한다. 실제 FITRUS API와 실제 진동기는 연결되지 않았다.

## 3. 제품의 진실과 금지 표현

| 항목 | 현재 가능한 표현 | 현재 금지되는 표현 |
|---|---|---|
| 측정 데이터 | `샘플 데이터`, `서버에 저장된 표준 측정값`, `최근 4건 평균` | `실시간 FITRUS 동기화 완료` |
| 알고리즘 | `pilot-0.6.0 연구용 시뮬레이션 규칙` | `임상 검증`, `의료 처방` |
| 기기 | `Mock 설정 준비`, `시연 실행`, `실제 기기 미연결` | `실장비 전송 완료`, `실제 진동 실행 중` |
| 상태 | `사용 가능`, `담당자 확인 필요`, `사용 중지` | 색상만으로 상태 전달 |

## 4. 올바른 동작 규약

### 입력 완전성

- 동일 참여자·동일 기기의 품질 검사를 통과한 측정값이 정확히 4건이어야 한다.
- 나이, 성별, 키가 유효 범위 안에 있어야 한다.
- 안전 문진 3문항에 각각 `예` 또는 `아니요`를 명시적으로 선택해야 한다.
- 답하지 않은 문항은 `아니요`가 아니라 `미확인`이다.

### 상태 결정

```text
문진 미완료 ───────────────→ REVIEW (명령 생성 금지)
통증/어지럼/사용보류 있음 ─→ BLOCKED (추천값·명령 생성 금지)
측정값 검증 실패/BMI 검토 ─→ REVIEW (명령 생성 금지)
모든 검증 통과 ────────────→ READY (Mock 명령 미리보기만 허용)
```

### 명령 생성

- `READY`이며 문진이 완료됐을 때만 활성화한다.
- 한 번 누르면 동일 입력의 명령 미리보기를 생성하고 중복 생성을 막는다.
- 프로필이나 문진을 변경하면 기존 명령은 즉시 폐기한다.
- Mock에서는 실행·완료를 항상 `시연`으로 표시하며 실제 장치 출력처럼 표현하지 않는다.

## 5. 컴포넌트 연결 계약

| UI 컴포넌트 | 입력 | 로직/서비스 | 출력 | 실패 시 |
|---|---|---|---|---|
| 대상자 정보 | 나이·성별·키 | `ParticipantProfile` | 알고리즘 프로필 | 유효범위 안내, Mock 허가 금지 |
| 측정 세트 | BIA 4건 | `selectLatestValidMeasurements` | 항목별 평균·경고 | REVIEW |
| 안전 문진 | 3개 명시 응답 | `SafetyCheck` | 안전 경고 | 미응답 REVIEW, 위험 응답 BLOCKED |
| 추천 패널 | 프로필+측정+문진 | `calculateRecommendation` | 시간·Hz·강도·근거 | REVIEW 또는 추천 미생성 |
| 실행 패널 | READY 추천 | `DeviceGateway` | 일회성 Mock 허가와 시뮬레이션 세션 | 실제 장비 출력 금지 |

데이터 흐름:

```text
Profile ─┐
BIA 4건 ─┼─→ 4건 검증/평균 ─→ 파일럿 보정 ─→ READY/REVIEW/BLOCKED
문진 3건 ┘                                      │
                                                └─ READY만 Mock 명령 생성
```

## 6. 모바일 정보 구조

첫 화면은 브랜드 소개가 아니라 `판단 → 확인 → 행동`을 위한 작업면이다.

1. 앱 바: 화면 목적(`오늘의 진동 설정`)과 참여자 코드
2. 시스템 상태: 측정 데이터 출처와 장치 상태를 아이콘+문구로 병렬 표시
3. 오늘 설정: 강도·시간·주파수를 같은 행에서 비교하고 READY/REVIEW/BLOCKED 표시
4. 계산 입력: 나이·성별·체지방 평균과 4회 평균 근거
5. 사용 전 상태 확인: 3문항 진행률과 명시적 예/아니요
6. 고정 하단 행동: `설정 보내기 → 시작 → 즉시 중지`로 상태에 따라 하나만 표시
7. 사용 후 확인: 체감 강도·통증·어지럼을 저장한 뒤 다음 사용으로 이동

원본 4건, 전체 생체신호, 보정 조건, JSON 명령은 하단 시트에 둔다. 실행 중에는 문진과 설정 카드를 숨기고 남은 시간·적용값·즉시 중지만 남긴다.

## 7. 시각 언어

방향은 `차분한 보조기기 제어 화면`이다.

- 흰색 작업면, 옅은 회색 배경, 짙은 남색 텍스트
- 청록은 핵심 행동과 포커스에만 제한적으로 사용
- 상태는 좌측 굵은 선, 아이콘, 제목, 설명을 함께 사용
- 핵심 요약 카드 하나와 안전 확인 카드 하나만 기본 화면에 사용
- 모서리 반경은 8~14dp, 그림자·그라데이션·장식성 배지는 사용하지 않음
- 숫자는 표 형식과 정렬을 사용해 비교 가능하게 표시
- 한 화면에 하나의 주 행동만 제공
- 상태는 색상뿐 아니라 아이콘과 쉬운 문구를 함께 제공
- 추천값과 사용자가 낮춘 적용값을 구분하고, 자동 추천보다 높이는 조절은 허용하지 않음

## 8. 접근성·콘텐츠 기준

- 기본 본문 17px 이상, 핵심 숫자 28px 이상
- 모든 누름 영역 최소 48×48dp, 인접 목표 사이 약 8dp 확보
- 본문 명암비 WCAG AA 이상, 상태를 색상만으로 전달하지 않음
- 질문은 한 문장에 한 판단만 포함하고 `예/아니요`를 명시
- 오류는 무엇이 잘못됐는지와 다음 행동을 함께 안내
- 중앙 정렬 본문, 긴 대문자, 전문 용어, 불필요한 애니메이션을 피함
- `prefers-reduced-motion`을 존중하고 키보드 포커스를 명확히 표시
- 200% 글자 크기에서도 핵심 값과 행동이 잘리거나 겹치지 않음
- 실행 중 자동으로 사라지는 필수 안내를 사용하지 않음

## 9. 품질 벤치마크

- W3C Older Users: 대비 감도, 미세 운동, 기억·집중 변화까지 고려  
  https://www.w3.org/WAI/older-users/
- Apple HIG Accessibility: 익숙한 상호작용, 충분한 제어 크기, 색상 이외의 상태 표현
  https://developer.apple.com/design/human-interface-guidelines/accessibility
- W3C Developing for Older People: 명확한 제목·레이블, 일관된 구조, 읽기 쉬운 텍스트  
  https://www.w3.org/WAI/older-users/developing/
- Android Accessibility: 최소 48dp 터치 목표  
  https://support.google.com/accessibility/android/answer/7101858?hl=en
- GOV.UK Error Message: 문제와 해결 방법을 구체적으로 표현  
  https://design-system.service.gov.uk/components/error-message/
- NHS Service Manual: 접근 가능한 공통 패턴과 실제 사용자 중심 검증  
  https://service-manual.nhs.uk/

## 10. 검증 체크리스트

- [ ] 첫 화면에서 Mock·실기기 미연결 상태가 보인다.
- [ ] 문진을 하나라도 답하지 않으면 버튼이 비활성화된다.
- [ ] 위험 응답을 선택하면 BLOCKED이며 추천값이 사라진다.
- [ ] 모든 문진에 `아니요`를 선택하면 READY가 된다.
- [ ] 공유 fixture에서 LOW/MEDIUM/REFERENCE 단계가 각각 30%/40%/50%로 재현된다.
- [ ] 프로필 변경 시 평균 일관성 검사가 다시 실행된다.
- [ ] 기존 명령은 입력 변경 즉시 사라진다.
- [ ] 버튼은 실제 전송이 아닌 Mock 명령 미리보기 생성으로 읽힌다.
- [ ] 320px 너비, 200% 확대, 키보드만으로 핵심 흐름을 완료한다.
- [ ] 빌드·린트·알고리즘 테스트가 통과한다.
- [ ] 고령 사용자 또는 대리 사용자 3명 이상에게 과업 기반 사용성 테스트를 진행한다.

## 11. 다음 단계

1. Flutter 화면을 고령 사용자 또는 대리 사용자에게 과업 기반으로 테스트한다.
2. 실제 사용에서 막힌 표현·터치·순서를 기록하고 디자인 규약에 반영한다.
3. FITRUS 실제 응답 fixture를 계약 테스트에 추가한다.
4. 실제 기기 명세와 교정표, 별도 안전 승인이 확보된 뒤에만 실장비 연결·ACK·실행·중지 상태를 추가한다.
