# 프로젝트 개요

> **Owner:** 제품·도메인 담당 · **Approver:** 도메인·QA 담당
>
> **Update trigger:** 사용자 문제, 핵심 흐름, 안전 제약 또는 완료 정의가 바뀔 때
>
> **Must not contain:** 구현 진행률, 예정 명령, 실행하지 않은 검증 결과

## 왜 만드는가

고령 사용자가 여러 체성분 수치를 직접 해석하지 않아도 최근 측정값과 안전 상태를 확인하고, 진동 설정이 어떻게 계산됐는지 이해한 뒤 시연 장치를 조작할 수 있게 한다.

## 올바른 동작

- 로그인한 참여자와 같은 FITRUS 기기의 유효한 최근 측정값 4건만 사용한다.
- 원본, 평균, 계산 기준과 결과를 화면에 구분해 표시한다.
- 공급사 `골격근량` 정의가 `ASM` 또는 `SMM`으로 확인된 경우에만 해당 경로를 사용한다. `UNKNOWN`을 사용자가 임의 선택해 해석하지 않는다.
- 통증, 어지럼, 사용 보류, 데이터 오류에서는 전송을 차단한다.
- 앱 결과와 서버 재계산이 다르면 세션을 만들지 않는다.
- 실제 장비 명세와 교정값이 없으므로 물리 출력은 항상 비활성화한다.
- `pilot-0.8.0`은 6개 신체 부위 기준값과 성별·나이·체지방 연구용 보정 과정을 Mock 화면에 표시한다.
- 로그인 직후 프로필 아래에서 통증·어지럼·사용 보류와 연구용 시뮬레이션 주의사항을 먼저 확인한다.
- 다음 화면에서 DB에 저장된 최신 골격근량과 직전 기록의 변화를 보여 주고, SMM/키² 연구 경계를 `낮음·중간·높음`으로 표시한다.
- 장치 보내기 흐름에서는 인체 부위와 API가 승인한 시간·주파수·출력을 보여 준다. 과거 `12/16/20Hz`, `3/4/5분` 등급 매핑은 연구용 참고값으로만 표시하며 실제 전송값으로 사용하지 않는다.

## 책임

| 구성요소 | 책임 |
|---|---|
| `mobile-app` | 입력·통계·연구 후보 표시, 확인된 정의 고정, 문진과 Mock 시뮬레이터 조작 |
| `backend-api` | 비밀키 보호, 인증, 데이터 소유권, 추천 재계산, 시연 세션 감사 기록, `SqlDatabase` 저장소 포트 |
| `shared-contracts` | 앱과 서버의 HTTP·데이터 계약 및 공통 검증 fixture |
| `deployment/aws` | AWS 담당자에게 필요한 환경변수·DB·안전 기본값 인계 |

## 완료 기준

Mock 환경에서 `로그인 → 프로필·주의사항 → 최신/직전 측정 비교와 연구 등급 → 인체 부위·API 설정 확인 → 안전 문진 → 시뮬레이터 전송 → 시작 → 중지 → 피드백` 흐름과 오류·중복 방지 테스트가 통과하면 시연 버전 완료로 본다. 실제 API와 하드웨어는 각각의 공식 계약과 교정 검증 후 별도 완료 판정을 한다.

`pilot-0.7.0` 구현 커밋 `2d15e9f`의 [GitHub Actions run 34459978932](https://github.com/Dowon-Kang/vibecare-pilot/actions/runs/34459978932)에서 백엔드 typecheck와 Vitest 43/43, Flutter format·analyze·전체 테스트 38/38과 debug APK 빌드가 통과했다. APK는 사용자 PC에서 반복 생성하지 않고 CI에서 검증했다. 이 결과는 임상 효과나 실장비 안전을 의미하지 않는다.
